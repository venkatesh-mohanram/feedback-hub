{
  "name": "feedback-hub",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Browser-only feedback prototype served by a small Node.js application.",
  "scripts": {
    "start": "node src/server.mjs",
    "test": "node --test src/*.test.mjs",
    "check": "node --check src/app.mjs && node --check src/feedback-core.mjs && node --check src/storage.mjs && node --check src/server.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
